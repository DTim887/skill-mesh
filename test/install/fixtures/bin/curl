#!/bin/sh
# 测试用假 curl：不发真实网络请求，按环境变量控制返回内容/成功失败（见 install.sh.test.sh）。
if [ "${FAKE_CURL_FAIL:-}" = "1" ]; then
  # 模拟 curl -f 在 HTTP 错误（如 404）时的非零退出码
  exit 22
fi
cat "${FAKE_CURL_RESPONSE_FILE:?FAKE_CURL_RESPONSE_FILE not set}"
